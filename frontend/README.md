# Richwell Portal Frontend

This Vite + React project provides the client shell for the Richwell portal. The placeholder UI
confirms that the application boots correctly and will be extended in subsequent phases.

## Local development

```bash
npm install
npm run dev
```

The development server runs at http://localhost:5173.
